import { BigNumber as OldBigNumber } from '../utils/bignumber';
import { NewPath, Swap, SwapTypes } from '../types';
import { BigNumber } from '@ethersproject/bignumber';
export declare const getBestPaths: (paths: NewPath[], swapType: SwapTypes, totalSwapAmount: BigNumber, inputDecimals: number, outputDecimals: number, maxPools: number, costReturnToken: BigNumber) => [Swap[][], OldBigNumber, OldBigNumber, OldBigNumber];
