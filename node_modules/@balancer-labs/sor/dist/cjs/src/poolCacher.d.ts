import { PoolDataService, SubgraphPoolBase, GraphQLArgs } from './types';
export declare class PoolCacher {
    private readonly poolDataService;
    private pools;
    private _finishedFetching;
    constructor(poolDataService: PoolDataService);
    get finishedFetching(): boolean;
    getPools(useBpts?: boolean): SubgraphPoolBase[];
    fetchPools(queryArgs?: GraphQLArgs): Promise<boolean>;
}
