import { BigNumber } from '@ethersproject/bignumber';
import { SwapTypes, NewPath } from '../types';
export declare function calculatePathLimits(paths: NewPath[], swapType: SwapTypes): [NewPath[], BigNumber];
export declare function getLimitAmountSwapForPath(path: NewPath, swapType: SwapTypes): BigNumber;
