import { BigNumber } from '@ethersproject/bignumber';
import { DerivedGyroEParams, GyroEParams } from './gyroEMath/gyroEMathHelpers';
export declare function calculateDerivedValues(p: GyroEParams): DerivedGyroEParams;
export declare function sqrtArbitraryDecimal(input: BigNumber, decimals: number): BigNumber;
