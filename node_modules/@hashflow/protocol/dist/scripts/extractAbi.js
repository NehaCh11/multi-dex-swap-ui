"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const fs_1 = __importDefault(require("fs"));
const artifacts_1 = __importDefault(require("./lib/artifacts"));
const directory = 'abi/';
if (!fs_1.default.existsSync(directory)) {
    fs_1.default.mkdirSync(directory);
}
const allContracts = artifacts_1.default;
const allContractNames = Object.keys(allContracts);
for (const contractName of allContractNames) {
    const contract = allContracts[contractName];
    const abi = contract.abi;
    const filename = `${contractName}.json`;
    const json = JSON.stringify(abi, null, 4);
    const filePath = `${directory}${filename}`;
    fs_1.default.writeFileSync(filePath, json);
    console.log(`Wrote ${filePath}`);
}
