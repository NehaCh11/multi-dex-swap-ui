import { Contracts } from '../Contracts';
import { IRenovaQuest } from '../contracts/hashverse';
import { IERC721 } from '../contracts/hashverse/IERC721';
export declare class Hashverse {
    private contracts;
    constructor(contracts: Contracts);
    getQuest(questAddress: string): IRenovaQuest;
    getERC721Token(tokenAddress: string): IERC721;
}
