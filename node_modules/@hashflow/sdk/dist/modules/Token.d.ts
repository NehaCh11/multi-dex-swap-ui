import { Contracts } from '../Contracts';
import { IERC721 } from '../contracts/hashverse/IERC721';
import { ERC20, IWETH } from '../contracts/protocol';
export declare class Token {
    private contracts;
    constructor(contracts: Contracts);
    getToken(tokenAddress: string): ERC20;
    getERC721Token(tokenAddress: string): IERC721;
    getWethContract(weth: string): IWETH;
}
