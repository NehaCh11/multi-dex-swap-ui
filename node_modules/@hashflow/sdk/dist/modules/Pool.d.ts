import { Contracts } from '../Contracts';
import { IHashflowPool } from '../contracts/protocol';
export declare class Pool {
    private contracts;
    constructor(contracts: Contracts);
    getPool(poolAddress: string): IHashflowPool;
}
