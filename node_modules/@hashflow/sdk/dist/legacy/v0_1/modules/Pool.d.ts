import { Contracts } from '../Contracts';
import { IHashflowSpotPubPool } from '../contracts/protocol';
import { IHashflowPool } from '../contracts/protocol/IHashflowPool';
import { IHashflowPrivPool } from '../contracts/protocol/IHashflowPrivPool';
import { address } from '../types';
export declare class Pool {
    private contracts;
    constructor(contracts: Contracts);
    getPool(poolAddress: address): IHashflowPool;
    getPrivPool(poolAddress: address): IHashflowPrivPool;
    getSpotPubPool(poolAddress: address): IHashflowSpotPubPool;
}
