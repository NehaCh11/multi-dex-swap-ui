import { TransactionLike } from 'ethers';
import { Contracts } from '../Contracts';
export declare class Optimism {
    private contracts;
    constructor(contracts: Contracts);
    estimateOptimismL1GasFee(transaction: TransactionLike): Promise<bigint>;
}
